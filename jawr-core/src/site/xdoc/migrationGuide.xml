<document>

	<properties>
		<title>Jawr :: Migration Guide</title>
	</properties>

	<body>

		<section name="Migration Guide from 3.3.x to 3.5">
			<ul>
				<li>
					<strong>Generators :</strong>
					<br />
					Since the version 3.5, Jawr allows the users to define their custom
					GeneratorPathResolver. This means that the users can now define generators, wich will be triggered by prefix path. (like built-in
					'jar:' or 'message:' prefixes) or with for example a specific path suffix (like '*.less' resources).

					In the version 3.3.x, the generator should implement the method
					"getMappingPrefix". This method has been replaced by "ResourceGeneratorResolver getResolver()" method.
					Jawr defines built-in ResourceGeneratorResolvers :
					<ul>
						<li>net.jawr.web.resource.bundle.generator.resolver.PrefixedPathResourceGeneratorResolver : For prefix path resolver
						</li>
						<li>net.jawr.web.resource.bundle.generator.resolver.SuffixedPathResourceGeneratorResolver : For suffix path resolver
						</li>
					</ul>
					To replace the following your custom generator, you need to replace
					<p>
					<div class="source">
					<pre>
						public class SampleJsGenerator extends AbstractJavascriptGenerator {

							/* (non-Javadoc)
							 * @see net.jawr.web.resource.bundle.generator.ResourceGenerator#getResolver()
							 */
							public String getMappingPrefix() {
								return "foo";
							}

						    ...
						}
					</pre>
					</div>
					</p>
					<p> With the following
					<div class="source">
					<pre>
						public class SampleJsGenerator extends AbstractJavascriptGenerator {

							/* (non-Javadoc)
							 * @see net.jawr.web.resource.bundle.generator.ResourceGenerator#getResolver()
							 */
							public ResourceGeneratorResolver getResolver() {
								return new PrefixedPathResourceGeneratorResolver("foo");
							}

						    ...
						}
					</pre>
					</div>
					</p>  
				</li>
				<li>
					<strong>Jawr Project module :</strong> /* MAVEN */
				</li>
			</ul>
		</section>
	</body>

</document>